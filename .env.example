# Copie para .env e preencha os valores (nunca commite o .env)
ADO_PAT=seu_personal_access_token_azure_devops
ANTHROPIC_API_KEY=sua_api_key_anthropic   # opcional — apenas para o comando ask

# Ollama local (comando coach) — defaults já funcionam com ollama serve padrão
OLLAMA_HOST=http://localhost:11434
OLLAMA_MODEL=qwen2.5-coder:14b
